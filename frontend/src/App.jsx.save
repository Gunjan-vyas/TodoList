import { useState } from 'react'
import reactLogo from './assets/react.svg'
import viteLogo from '/vite.svg'
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <>
          <img src={viteLogo} className="logo" alt="Vite logo" />

</      <div>
        <a href="https://vitejs.dev" target="_blank">
gjfg          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h      <div>
        <a href="https://vitejs.dev" target="_blank">
gjfg          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

      <div>
        <a href="https://vitejs.dev" target="_blank">
gjfg          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

      <div>
        <a href="https://vitejs.dev" target="_blank">
gjfg          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

      <div>
        <a href="https://vitejs.dev" target="_blank">
gjfg          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

      <div>
        <a href="https://vitejs.dev" target="_blank">
gjfg          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

      <div>
        <a href="https://vitejs.dev" target="_blank">
gjfg          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

      <div>
        <a href="https://vitejs.dev" target="_blank">
gjfg          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

      <div>
        <a href="https://vitejs.dev" target="_blank">
gjfg          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}

          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <<img src={reactLogo} className="logo react" alt="React logo" />
        <
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
  )
}


  )
}

export default App
